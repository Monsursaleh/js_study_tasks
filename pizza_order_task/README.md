# Simple pizaa order app for praticing diffrent kinds of events

# use radio, checkbox, text, select for makign the apps

# it will calculate during the changes not

# use 'change' event lister mostly
